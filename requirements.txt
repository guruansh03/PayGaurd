# Core
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.0

# Deep learning
torch==2.3.1

# Approximate kNN for full-data LOF (optional but strongly recommended)
# faiss-cpu speeds up LOF from O(n*k*log n) exact to O(nprobe*k) approximate.
# Without it, LOF falls back to sklearn BallTree on full data (~5 min train, ~2s inference).
faiss-cpu>=1.7.4

# Explainability
shap==0.45.1

# Visualization
plotly==5.22.0
matplotlib==3.9.0
seaborn==0.13.2

# Web app
streamlit==1.35.0

# Utilities
scipy==1.13.1

# C-03: Screenshot OCR
pillow>=10.0.0
pytesseract>=0.3.10
# System dep: tesseract-ocr binary required
# Streamlit Cloud: add 'tesseract-ocr' to packages.txt
